bin/
obj/
*.user
.vs/
TestResults/
# publish + installer output (dotnet publish -o artifacts, wix build)
artifacts/
*.msi
# local data: database, token cache, downloaded models
data/
*.db
*.db-*
msal_cache*
eval/*.local.json
app-registration.json

# scratch output from regenerating brand rasters
assets/generated/

# wingetcreate drops a winget-pkgs folder layout here; the templates live in packaging/winget
manifests/
